line-length = 100
target-version = "py311"

[lint]
# E/W basics, F (pyflakes real bugs), I (import order), B (bugbear),
# C4 (comprehension simplification)
select = ["E4", "E7", "E9", "F", "I", "B", "C4"]
# B008: FastAPI's File()/Form() in argument defaults is the framework idiom
ignore = ["B008"]
